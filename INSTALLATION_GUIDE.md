# 📦 دليل تسليم نظام إدارة مخزون الكافيه
## Cafe Stock Management System - Installation Guide

---

## 🎯 الخطوة 1: إنشاء جدول Google Sheets

### 1.1 إنشاء الجدول:
1. اذهب إلى [Google Sheets](https://sheets.google.com)
2. اضغط على **+ جديد** أو **New**
3. اختر **جدول بيانات فارغ** (Blank spreadsheet)
4. سمّ الجدول: `Cafe Stock Management`

### 1.2 إنشاء الشيتات المطلوبة:
قم بإنشاء الشيتات التالية (تبويبات في الأسفل):

#### ✅ شيت 1: **Projects**
| العمود A | العمود B |
|----------|----------|
| ID       | اسم المشروع |
| 1        | مشروع تجريبي |

#### ✅ شيت 2: **Products**
| العمود A | العمود B    | العمود C      | العمود D |
|----------|-------------|---------------|----------|
| ID       | الباركود    | اسم المنتج    | الوحدة   |
| 1        | 123456      | منتج تجريبي   | قطعة     |

#### ✅ شيت 3: **Settings**
| العمود A                 | العمود B     |
|--------------------------|--------------|
| كلمة المرور المؤقتة       |              |
|                          | Cafe2025     |

**ملاحظة:** الشيتات `Stock_Out` و `Stock_In` سيتم إنشاؤها تلقائياً عند أول استخدام.

---

## 🔑 الخطوة 2: الحصول على معرف الجدول (Spreadsheet ID)

### 2.1 استخراج المعرف:
1. افتح جدول Google Sheets الذي أنشأته
2. انظر إلى الرابط في المتصفح
3. الرابط يكون بهذا الشكل:
   ```
   https://docs.google.com/spreadsheets/d/1Ex80V03d6_UDSw35FWToCzfdGuWKPiVxjL76YprklTw/edit
   ```
4. انسخ الجزء بين `/d/` و `/edit`
5. هذا هو معرف جدولك (في المثال أعلاه: `1Ex80V03d6_UDSw35FWToCzfdGuWKPiVxjL76YprklTw`)

### 2.2 احفظ المعرف:
📝 **احتفظ بهذا المعرف - ستحتاجه في الخطوة التالية**

---

## ⚙️ الخطوة 3: إعداد Google Apps Script

### 3.1 فتح محرر الأكواد:
1. من داخل جدول Google Sheets
2. اذهب إلى القائمة: **Extensions** (الإضافات) → **Apps Script**
3. سيفتح محرر الأكواد في تبويب جديد

### 3.2 نسخ الكود:
1. احذف أي كود موجود في المحرر
2. افتح ملف `google-apps-script.js` من المشروع
3. انسخ **جميع** الكود
4. الصق الكود في محرر Google Apps Script

### 3.3 تعديل معرف الجدول:
1. في السطر 10 تقريباً، ابحث عن:
   ```javascript
   const SPREADSHEET_ID = 'ضع_هنا_معرف_جدولك_الخاص';
   ```
2. استبدل `ضع_هنا_معرف_جدولك_الخاص` بمعرف جدولك الذي نسخته في الخطوة 2
3. مثال:
   ```javascript
   const SPREADSHEET_ID = '1Ex80V03d6_UDSw35FWToCzfdGuWKPiVxjL76YprklTw';
   ```
4. **كرر نفس الخطوة في السطر 100 تقريباً** (داخل دالة `doPost`)

### 3.4 حفظ المشروع:
1. اضغط على أيقونة القرص 💾 أو Ctrl+S
2. سمّ المشروع: `Cafe Stock Management API`
3. اضغط **موافق** أو **OK**

---

## 🚀 الخطوة 4: نشر الـ Web App

### 4.1 إنشاء Deployment جديد:
1. في محرر Google Apps Script
2. اضغط على **Deploy** (نشر) → **New deployment** (نشر جديد)
3. اضغط على أيقونة الترس ⚙️ بجانب "Select type"
4. اختر **Web app**

### 4.2 إعدادات النشر:
- **Description**: `Cafe Stock API v1.0`
- **Execute as**: **Me** (مهم جداً!)
- **Who has access**: **Anyone** (مهم جداً!)

### 4.3 منح الصلاحيات:
1. اضغط **Deploy**
2. سيطلب منك منح صلاحيات
3. اضغط **Authorize access** (السماح بالوصول)
4. اختر حسابك في Google
5. اضغط **Advanced** (متقدم)
6. اضغط **Go to [Project Name] (unsafe)** - لا تقلق، هذا مشروعك الخاص
7. اضغط **Allow** (السماح)

### 4.4 نسخ رابط الـ Web App:
1. بعد النشر، ستظهر نافذة بها رابط
2. انسخ الرابط الذي يبدأ بـ:
   ```
   https://script.google.com/macros/s/AKfyc...../exec
   ```
3. **احتفظ بهذا الرابط - ستحتاجه في الخطوة التالية**

⚠️ **ملاحظة مهمة:** تأكد أن الرابط ينتهي بـ `/exec` وليس `/dev`

---

## 💻 الخطوة 5: إعداد صفحة الويب

### 5.1 فتح ملفات المشروع:
1. افتح مجلد المشروع `Cafe_stoke`
2. افتح ملف `0541.html` في المتصفح

### 5.2 إدخال رابط Google Apps Script:
1. في صفحة الإعدادات
2. الصق الرابط الذي نسخته في الخطوة 4.4
3. اضغط **💾 حفظ الرابط**
4. اضغط **🧪 اختبار الاتصال** للتأكد من نجاح الربط

### 5.3 التحقق من كلمة المرور:
- يجب أن تظهر كلمة المرور المؤقتة تلقائياً
- إذا لم تظهر، اضغط **🔄 تحديث**

---

## ✅ الخطوة 6: اختبار النظام

### 6.1 اختبار إخراج المخزون:
1. افتح ملف `Stock_Out.html`
2. اختر مشروع
3. أضف منتج
4. اضغط إرسال
5. تحقق من ظهور البيانات في شيت `Stock_Out`

### 6.2 اختبار إدخال المخزون:
1. افتح ملف `Stock_In.html`
2. اختر نفس المشروع
3. يجب أن تظهر المنتجات المستخدمة في ذلك المشروع
4. أضف كمية
5. اضغط إرسال
6. تحقق من ظهور البيانات في شيت `Stock_In`

---

## 📝 ملخص سريع للتسليم

عند تسليم المشروع لشخص آخر، اطلب منه:

1. ✅ إنشاء جدول Google Sheets جديد
2. ✅ إنشاء 3 شيتات: `Projects`, `Products`, `Settings`
3. ✅ نسخ معرف الجدول من الرابط
4. ✅ فتح Apps Script من القائمة Extensions
5. ✅ نسخ كود `google-apps-script.js` بالكامل
6. ✅ تعديل `SPREADSHEET_ID` في موضعين (السطر 10 والسطر 100)
7. ✅ حفظ المشروع باسم `Cafe Stock Management API`
8. ✅ Deploy → New deployment → Web app
9. ✅ Execute as: Me, Who has access: Anyone
10. ✅ نسخ رابط الـ Web App (ينتهي بـ /exec)
11. ✅ فتح `0541.html` ولصق الرابط
12. ✅ اختبار الاتصال

---

## 🔧 استكشاف الأخطاء

### ❌ خطأ: "لم يتم العثور على الشيت"
**الحل:** تأكد من إنشاء الشيتات بالأسماء الصحيحة:
- `Projects`
- `Products`
- `Settings`
- (لا تنس حرف s في النهاية)

### ❌ خطأ: "Permission denied"
**الحل:** 
1. تأكد من اختيار "Execute as: Me"
2. تأكد من منح جميع الصلاحيات عند النشر

### ❌ خطأ: "فشل الاتصال"
**الحل:**
1. تأكد أن الرابط ينتهي بـ `/exec` وليس `/dev`
2. تأكد من اختيار "Who has access: Anyone"
3. جرب إنشاء Deployment جديد

### ❌ كلمة المرور لا تظهر
**الحل:**
1. تأكد من وجود شيت اسمه `Settings`
2. تأكد من وجود كلمة المرور في الخلية `B2`
3. اضغط زر "🔄 تحديث"

---

## 📞 دعم فني

للمزيد من المساعدة:
- راجع ملف `README.md` في المشروع
- راجع ملف `PASSWORD_SETUP.md` لتفاصيل كلمة المرور
- تحقق من console.log في المتصفح (F12)

---

## 🎉 تم بنجاح!

الآن النظام جاهز للاستخدام بشكل مستقل لأي شخص مع جدول Google Sheets الخاص به!

**تاريخ الإنشاء:** 12 أكتوبر 2025  
**الإصدار:** 1.0
